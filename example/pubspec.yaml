name: example
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.3+3

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  adaptive_navbar: 
    path : ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
